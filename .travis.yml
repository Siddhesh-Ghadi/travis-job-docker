arch: 
  - ppc64le
sudo: required
services:
  - docker

script:
  - sudo apt install maven
  - docker run -t ubuntu:18.04 bash -c 'apt update -y && apt install -y maven'
  - docker run -t centos:7 bash  -c 'yum update -y && yum install -y maven'

